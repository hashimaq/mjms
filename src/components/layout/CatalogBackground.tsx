/** Page atmosphere — flat brand paper only; hero shapes live in the hero section. */
export function CatalogBackground() {
  return (
    <div className="catalog-bg" aria-hidden>
      <div className="catalog-bg-paper" />
    </div>
  );
}
